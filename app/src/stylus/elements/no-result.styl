.no-result
    display flex
    min-height 400px
    justify-content center
    align-items center
    +element(message)
        font-size 19px
        font-family Roboto, sans-serif
        color $grayTextColor
        line-height 1.25
        font-weight 600
