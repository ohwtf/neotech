$set_font-size(value)
  font-size value /* fallback */

  if (value is not inherit)
    font-size $remify(value)

$remify(value)
  u = unit(value)

  if (u is 'px') {
    return unit(value/16, 'rem')
  } else {
    return unit(value, u)
  }
